ANTHROPIC_API_KEY=sk-ant-...

# Soft cap on research/API calls per topic before the user must explicitly opt to continue
RESEARCH_CALL_SOFT_CAP=40

# Key used to sign session cookies. Generate a real value with:
# python3 -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=change-me
