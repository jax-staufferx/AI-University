{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [{"name": "ANTHROPIC_API_KEY", "description": "Anthropic API key for Claude (research, teaching, grading)", "requiredAtBoot": false}]
}
